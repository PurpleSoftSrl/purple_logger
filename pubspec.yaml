name: purple_logger
description: Enterprise-grade structured logger for Dart and Flutter. Provider pipeline, Zone-based scopes, filter rules, OpenTelemetry bridge, and zero-alloc guards.
version: 2.0.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0