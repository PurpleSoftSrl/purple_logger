name: purple_logger
description: Enterprise-grade structured logger for PurpleSoft Flutter plugins. Zero print() calls, hierarchical log levels, and production-ready defaults.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  logging: ^1.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0