name: purple_logger
description: Enterprise-grade structured logger for Dart and Flutter — provider pipeline, zone scopes, file rotation, OTel bridge.
version: 2.1.4
repository: https://github.com/purplesoftsrl/purple_logger
homepage: https://github.com/purplesoftsrl/purple_logger
topics: [logging, observability, opentelemetry, structured-logging]
license: AGPL-3.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
