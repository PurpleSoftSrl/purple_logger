name: purple_logger
description: Enterprise-grade structured logger for Dart and Flutter — provider pipeline, zone scopes, file rotation, OTel bridge.
version: 2.1.2
repository: https://github.com/purplesoftsrl/purple_logger'>=3.2.0 <4.0.0'

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

